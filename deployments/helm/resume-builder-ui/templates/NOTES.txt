resume-builder-ui has been deployed to namespace {{ .Release.Namespace }}.

Check rollout status:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "resume-builder-ui.fullname" . }}

{{- if .Values.ingress.enabled }}

Ingress path "/" routes to this service on port {{ .Values.service.port }}.
The "/api" and "/oauth2" paths live in the separate resume-builder-api
Ingress/release — nginx merges multiple Ingress objects that share the same
ingressClassName, so both coexist on the same host.
{{- end }}
